#!/bin/sh
set -euo pipefail
IFS=$'\n\t'

sed -i '/^\[comment\]: # \(.*pre-commit.*\)/q' ./README.md
echo '```Hack' >> README.md
tree -F -n -d -L 2 . >> README.md
echo '```' >> README.md
git add README.md
